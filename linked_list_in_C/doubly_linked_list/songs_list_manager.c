#include <stdio.h>
#include <stdlib.h>

/* ----- Data structure ----- */

typedef struct song_s
{
    char *title;
    int duration;
    struct song_s *next;
    struct song_s *prev;
} song_t;

/* ----- Function prototypes ----- */

/* (we'll add these as we build each function) */

int main(void)
{
    return (0);
}

/* ----- Function definitions ----- */

/* (we'll add these as we build each function) */